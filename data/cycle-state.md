# Cycle State

<!--
  AUTOMATED FILE — managed by the weekly-prospect-outreach workflow.

  The workflow reads `current_run` at the start of each Monday run and
  increments it at the end. It also flips `status` to `awaiting-reset`
  after run 4 completes.

  You should ONLY edit this file by hand in one situation: after you have
  refreshed data/active-contacts.md with 10 new contacts and you are ready
  to start a fresh 4-week cycle. In that case set:
      current_run: 1
      status: active

  Do not edit `current_run` mid-cycle — the workflow owns it.

  Valid values:
    current_run : 1, 2, 3, or 4
    status      : active | awaiting-reset
-->

current_run: 1
status: active
